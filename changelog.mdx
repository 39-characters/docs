---
title: "Changelog"
description: "Latest updates and improvements to Night City"
---

<Update label="2026-03-21" tags={["Fix"]}>
  ## README CLI command fix

  Fixed an incorrect CLI command in the README troubleshooting section — `mint update` has been replaced with the correct `mint upgrade` command, consistent with the same fix previously applied in [development](/development).
</Update>

<Update label="2026-03-19" tags={["Fix"]}>
  ## Configuration and content fixes

  Added the missing OpenAPI specification reference in `docs.json`, which is required for the API playground and auto-generated endpoint documentation to work correctly. Fixed an invalid CLI update command in [development](/development) — `npm mint update` has been replaced with the correct `mint upgrade` command. Updated a broken internal link example in [Markdown syntax](/essentials/markdown) that referenced a non-existent `/writing-content/text` path. Also standardized the site name to "Night City" across the configuration and corrected inconsistent JSON formatting in the navigation structure.
</Update>

<Update label="2026-03-18" tags={["Fix"]}>
  ## Applied documentation quality fixes

  All fixes described in the March 17 changelog entry have been applied to the codebase. Changes include correcting the X social link in the footer, reducing the site font weight from 1000 to 400 for readability, removing test values from the navigation configuration, fixing a typo in [reusable snippets](/essentials/reusable-snippets), adding a missing language tag in [code blocks](/essentials/code), adding alt text to an image in [images and embeds](/essentials/images), cleaning up placeholder content in [Markdown syntax](/essentials/markdown), and updating broken links in [global settings](/essentials/settings). Additionally, fixed MDX parsing errors in the project configuration that were causing validation failures.
</Update>

<Update label="2026-03-17" tags={["Fix"]}>
  ## Documentation quality improvements

  Fixed broken internal links across multiple pages including incorrect references to `/api-playground/demo` and `/writing-content/embed`. Corrected the X social link in the footer, which was previously pointing to the wrong URL. Adjusted the site font weight from 1000 to 400 for better readability, and removed test values from the navigation configuration. Several content fixes were also applied, including a typo correction in [reusable snippets](/essentials/reusable-snippets), a missing language tag on a code block in [code blocks](/essentials/code), missing alt text on an image in [images and embeds](/essentials/images), and placeholder content cleanup in [Markdown syntax](/essentials/markdown).
</Update>

<Update label="2026-03-15" description="March 2026" tags={["Feature", "Update"]}>
  ## AI-powered documentation workflows

  You can now use your favorite AI coding tools to write and maintain Night City documentation. Setup guides are available for [Cursor](/ai-tools/cursor), [Claude Code](/ai-tools/claude-code), and [Windsurf](/ai-tools/windsurf). Each guide walks you through installing the Mintlify skill and configuring your editor for documentation workflows.

  ## Contextual code actions

  Documentation pages now include contextual actions that let you open code examples directly in Cursor, VS Code, ChatGPT, Claude, Perplexity, or via MCP. Look for the action menu on any code block to use it in your preferred tool.

  ## Plants API

  The Plants API is live with endpoints for [listing plants](/api-reference/endpoint/get), [creating plants](/api-reference/endpoint/create), [deleting plants](/api-reference/endpoint/delete), and receiving [webhook notifications](/api-reference/endpoint/webhook) when new plants are added. All endpoints use Bearer token authentication.

  ## Dropdown navigation

  The site navigation now uses a dropdown layout, making it easier to switch between Guides and API Reference sections. You can learn more about configuring navigation in the [navigation guide](/essentials/navigation).

  ## Site customization

  Night City now features a custom Lato font, grid background decoration, and branded color scheme. The navbar includes a **Support** contact link and a **Dashboard** button for quick access. Footer social links connect you to X, GitHub, and LinkedIn.

  ## Writing content guides

  New guides cover everything you need to author documentation: [Markdown syntax](/essentials/markdown) for text formatting, [code blocks](/essentials/code) with syntax highlighting, [images and embeds](/essentials/images) including video iFrames, and [reusable snippets](/essentials/reusable-snippets) for shared components across pages.

  ## Getting started documentation

  The docs site now includes a complete onboarding path with an [introduction](/index) overview, a step-by-step [quickstart](/quickstart) for deploying your first changes, and a [development](/development) guide for local preview and tooling setup. A [global settings](/essentials/settings) reference covers all available configuration options in `docs.json`.
</Update>
